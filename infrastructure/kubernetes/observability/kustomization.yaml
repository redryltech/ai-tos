# AI-TOS Observability Platform (Phase 0B.6) — ADR-0010
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
resources:
  - otel-collector.yaml
  - prometheus.yaml
  - alertmanager.yaml
  - loki.yaml
  - tempo.yaml
  - grafana.yaml
  - grafana-dashboards.yaml
  # servicemonitors.yaml — apply after Prometheus Operator CRDs are installed
commonLabels:
  app.kubernetes.io/part-of: ai-tos
  app.kubernetes.io/component: observability
